# Default Gemini API key used by server routes when a request does not send api_key.
GEMINI_API_KEY=

# Optional: enable the in-memory write rate limiter.
ENABLE_RATE_LIMIT=false

# Optional: set one of these when the app needs to fetch protected GCS-backed image URLs.
GOOGLE_APPLICATION_CREDENTIALS=
GOOGLE_SERVICE_ACCOUNT_JSON=
